import React from "react";
import { BoxLoading } from "react-loadingg";

const Loading = () => {
  return (
    <div>
      <BoxLoading color="#FF4500" size="large" />
    </div>
  );
};

export default Loading;
